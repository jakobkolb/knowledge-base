{{- if and .Values.mcp.secretEnv .Values.global.createSecrets }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "mcp-obsidian.fullname" . }}-mcp-secrets
  labels:
    {{- include "mcp-obsidian.labels" . | nindent 4 }}
type: Opaque
stringData:
  {{- range $key, $val := .Values.mcp.secretEnv }}
  {{ $key }}: {{ $val | quote }}
  {{- end }}
{{- end }}
